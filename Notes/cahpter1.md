fn main() -main function
println!() -for printing 
cargo new dir/programm_name_here for  creating new program
cd dir/programm_name_here for
cargo build for building program
cargo run builds and runs 


cargo.toml file looks like following
[package]
name = "number_guessing"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.2"

[dependencies] can be added by running cargo add dependencies_name


